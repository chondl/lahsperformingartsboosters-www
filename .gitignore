# Dependencies
node_modules/

# Astro build output
dist/
.astro/

# Brainstorm scratch (visual companion)
.superpowers/

# Camera originals (tens of MB each). Only the cropped, web-sized derivatives in
# public/images/ are version-controlled; keep the originals in the photo archive.
src/images/

# OS / editor
.DS_Store
*.log

# Env / secrets
.env
.env.*
.dev.vars

# Git worktrees
.worktrees/

# Wrangler local state
.wrangler/

# clearance (Markdown viewer) rendered previews — never commit
.clearance-rendered-preview*/
